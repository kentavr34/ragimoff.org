# АУДИТ 2026-09-02 — сайт ragimoff.org · блог · электронная книга «Klinik Psixiatriya»

Проведён: 2026-09-02, агент MiniMax Code (сессия аудита). Режим: **только чтение** —
ни один файл проекта в ходе проверки не изменялся.

Объект: `D:\Документы\ragimoff` (HEAD `33b6456`, 2026-08-29) + живой прод
`https://ragimoff.org`.

Оси оценки: **организация** · **завершённость разделов** · **стилистическое
оформление** · **мобильная адаптация**. Раздельно по трём контурам.

---

## 0. Методика и честные оговорки

Мерились три слоя, и выводы ниже опираются на все три, а не на один:

1. **Статика репозитория** — счётчики файлов, разбор ссылок, метатегов, грид-,
   inline-стилей, терминологических канонов. Скрипты и их вывод:
   `scratchpad_tmp/_audit_*.py`, `_book*_out.txt`, `_blog_out.txt`, `_last_out.txt`.
2. **Живой прод** — HTTP-коды путей, Lighthouse на `/ru/index.html`,
   performance trace.
3. **Реальный мобильный DOM** — страница на 390×844, замеренные box-модели,
   hit-таргеты, сайдбар, поиск.

Что осталось неизмеренным и не утверждается:

- **Визуальный обзор.** Скриншоты в этой сессии недоступны (модель не принимает
  изображения, запись превью вне workspace отклоняется). Мобильная вёрстка оценена
  **по DOM и CSS-правилам**, то есть по факту расчёта, а не по картинке. там,
  где нужна глаз-оценка (эстетика, ритм, «красиво ли»), в отчёте стоит явная пометка
  «нужен визуальный review владельца».
- **Содержательная медицинская валидация** книги не проводилась: аудит фиксирует
  структуру, полноту разделов и метрики, но не сверяет клинические утверждения
  с первоисточниками (это отдельный контур, `PROOFREADING.md` / `refcheck.py`).

### Журнал самопроверки: что в черновике оказалось неверно

Записано намеренно, чтобы следующая сессия не перепроверяла с нуля.

| черновик | факт | причина ошибки |
|---|---|---|
| «97 карточек на дерево, документация (`PROJECTS.json`, `CLAUDE.md`) отстала» | **104**, документация верна | regex `^\d[A-Z0-9]{3}\.html$` отсекал коды, начинающиеся с буквы: `GA34`, `HA00`, `HA01`, `HA02`, `HA03`, `HA20`, `HA40` |
| «единственная неполная карточка — 9 разделов из 11 не найдено в `6A25.html`» | `6A25` **не неполная** — это карточка с альтернативным планом (4256 слов, `ref-list` на месте) | проверка сверяла с типовым 11-раздельным шаблоном, а не с назначением карточки |
| «источников: медиана 6, 18 карточек с <5» — посчитано по 97 | **медиана 6, min 3 (8 карточек), max 57, 18 с <5 — по всем 104 × 4 дерева**, сумма 897, расхождений между деревьями 0 | первый счётчик считал число списков `<ol class="ref-list">`, а не число записей `<li>` внутри них |
| «`klinik-psixiatriya.html` — незаполненная витрина главного продукта, 43 слова, добить контентом» | это **meta-refresh редирект** (`:5`, `content="0;url=/klinik-psixiatriya/"`) вроде тех 24 заглушек в книге, которые я же и объявил сознательными. Контент ему не нужен; реальный дефект только в отсутствии `viewport`/`description` | объём текста посчитан без чтения назначения страницы; П0 понижен до П2 |
| «неименованные кнопки `mobile-nav-close` **и** `hero-search-btn`» | `mobile-nav-close` — да (только SVG). `hero-search-btn` — **нет**: у него есть видимая подпись `{{search_btn}}` → «AXTAR / ПОИСК / SEARCH» (`_partials/hero-search.html:4`) | Lighthouse выдаёт «имя не из `<button>`-текста» для кнопок со сложным содержимым; проверено по разметке партиалы |
| «`gp.html`, `wc.html`, `template.html` в корне и в `ru/`» | `gp.html` и `wc.html` — только в корне; в `ru/` из этого списка есть только `template.html` | список построен по аналогии с остальными страницами, без перечисления фактических файлов |
| «форма заказа книги теряет заказы так же молча, как B2B» — распространено на все четыре дерева | в сайте (167 страниц) так и есть; в книге (548 страниц) копия модалки отправляет на `api.ragimoff.org/api/book-order` и открывает WhatsApp с текстом заказа — теряется не заявка, а **сообщение о сбое** | reading только `_partials/kitab-modal.html`; копии книги генерировались другим скриптом (`add_kitab_btn.py`, ныне мёртвый) и уехали по содержанию |
| «`build_headers.py` стирает различие между 6E20 и 6E21 — две рубрики XBT-11 станут неразличимы» (первый вариант п. 1.13) | **сильно преувеличено.** Генератор просит снять только дублирующую **приставку**; официальное имя XBT-11 в каноне заканывается хвостовым квалифициратором (`…SİMPTOMLARSIZ` / `…SİMPTOMLARLA`), и различие сохраняется | вывод `build_headers.py` печатает строки **усечёнными до 96 символов**; сверка по этому печатному окну не видит хвоста. Расшифровка получилась только чтением `_codes_canon.json → header_source.rows` напрямую |

Вывод для будущих аудитов: счётчик, который не сходится с канонической
документацией проекта, надо подозревать первым; а «мало текста на странице» —
вторым, потому что страница может быть редиректом или заглушкой по замыслу.

---

## 1. Сводка

| контур | организация | завершённость | стиль | мобильная адаптация |
|---|---|---|---|---|
| **Сайт** ragimoff.org (171 html) | 7 | 6 | 7 | 6 |
| **Блог** (112 статей + 3 хаба × 3 языка) | 8 | 8 | 7.5 | 5 |
| **Книга** «Klinik Psixiatriya» (104 карточки × 4 языка) | 8.5 | **9** | 7 | **4.5** |

Одной фразой по каждому:

- **Сайт** — организован строго и правильно (партиалы + `build.py` + генераторы),
  но в проде лежат два чужих файла с чужой аналитикой и **сломана форма B2B,
  через которую приходят деньги**. Это портит всю картину.
- **Блог** — лучший по сборке контур: 115 страниц из одного шаблона, зеркальны
  на трёх языках, canonical/hreflang/sitemap чистые. Ломает оценка мобильность
  (одна inline-сетка превращает статьи в 105-пиксельные колонки) и отсутствие
  any-какой серии-навигации между частями.
- **Книга** — по содержанию почти готова (104 карточки × 4 языка, паритет
  1.03/0.99, `<html lang>` 161/161, 897 библиографических записей без единого
  расхождения между языками). **По мобильному чтению — худшая**: сайдбар с 341
  ссылкой и кнопками раскрытия 20×17 px, CLS 0.10 из-за подмены шрифта и поиск,
  который не находит `depresiy` и `sizofren`.

---

## 2. КОНТУР A — основной сайт

### 2.1 Организация — 7

**Хорошо.** Архитектура правильная и соблюдена: `_partials/` (header, mobile-nav,
footer, hero-search, kitab-modal) + `_i18n.json` + `build.py`. Реальное дерево:
60 страниц в корне, 56 в `ru/`, 55 в `en/` (AZ-эталон и два перевода).
`SKIP_DIRS`/`SKIP_FILES` (`build.py:65-68`) корректно отделяют книгу, `_partials`,
`backend`, `images` и эталонный `index.html`. Живых идемпотентных скриптов 25,
все перечислены в `CLAUDE.md` и работают от `Path(__file__).parent`.

**Плохо.**

1. **Опубликованный мусор.** `gp.html` (55 КБ) и `wc.html` (262 КБ) в корне —
   это скопированные страницы чужих сайтов (guilford.com, worldcat.org), **живые
   на проде (HTTP 200)** и несущие чужие сниппеты Google Analytics и New Relic.
   Оба отследили git (`git ls-files`), значит уезжают в публикацию. `template.html`
   и `ru/template.html` — рабочий шаблон, тоже tracked; в выдаче ему быть не должно.
2. **Документация состояния отстаёт от фактов.** `PROJECTS.json` держит
   `pages_count_approx` = az 60 / ru 55 / en 56 против фактических 60 / 56 / 55 и
   ссылается на удалённые 2026-08-09 объекты: `_supplements/chapters-v2/`,
   `_inject_abbr.py` и «4166 `<abbr>`», виджет `duzelis.*`, удалённый 2026-08-10.
3. **`backend/`** — 8 из 10 файлов мёртвые генераторы с несуществующим путём
   `C:\Users\SAM\Desktop\sayt2`; `data/prices.json` и `data/certs.json` никем
   не читаются. Всё это маскируется под живой код.
4. **Сирота `samira.html`** — вне `_partials/header.html`, вне `sitemap.xml`,
   вне `search-index.json`. Страница существует, но из навигации в неё не попасть.

### 2.2 Завершённость разделов — 6

**Форма B2B не работает.** `b2b.html:371-378`, `ru/b2b.html:356-363`,
`en/b2b.html:356-363`:

```js
function handleB2BForm(e) {
  e.preventDefault();
  const btn = e.target.querySelector('button[type="submit"]');
  btn.textContent = 'Göndərildi. Tezliklə əlaqə saxlayacağıq.';
  ...
}
```

Ни `fetch`, ни `submitToAPI`, ни `mailto`. Рабочий паттерн в проекте есть и
используется в других формах: `shared.js:250-267` (`submitRegistration`,
сначала `api.ragimoff.org`, фолбэк на Apps Script) и `shared.js:271-294`
(честное сообщение об ошибке). Тот же класс бага уже описан комментарием в
`shared.js:219-224`. **Следствие: 100 % B2B-лидов теряются молча, клиент видит
«отправлено».** Это P0.

**Дрейф эталона `index.html`** — 6 мест (`index.html:96-106, :160-167, :170,
:100 vs :162, :171`), где разметка уехала от партиалов. Лечится штатно:
`build.py --migrate`, но трогать эталон можно только с явного разрешения владельца
(правило `CLAUDE.md`).

**`klinik-psixiatriya.html`** — страница входа в книгу со стороны сайта. Это
**meta-refresh редирект** (`:5`, `content="0;url=/klinik-psixiatriya/"`), а не
контентная страница: контента от неё не требуется, и первичная оценка
«43 слова — пустая витрина главного продукта» снята (см. журнал самопроверки, §0).
Дефект меньше и другого рода: у страницы, на которую ведут внешние ссылки и которая
стоит в sitemap, нет `viewport` и нет `description`.

**JSON-LD есть только в AZ-дереве**: 56 из 167 страниц сайта, **0 из 55 в `ru/`
и 0 из 55 в `en/`**. `twitter:card` — 0 из 167.

### 2.3 Стилистическое оформление — 7

- **Два Instagram и два Telegram в подвале** (`_partials/footer.html:12-15` против
  `:44-49`) — рассинхрон одной и той же партиалы.
- **Три ссылки в подвале ведут на `tehsil.html`** вместо `program-umumi.html`,
  `program-klinik.html`, `program-praktikum.html` (`_partials/footer.html:33-35`).
  Меню программ в итоге дублирует одну страницу.
- `target="_blank"` без `rel="noopener"` — по всему подвалу.
- **5511 инлайновых `style=`** на сайте; `gtc-dark.css` — 339 `!important` на 243
  правила, то есть почти каждое правило тёмной темы перебивает с усилением.
- Ритм разделов и контраст исправлены корректно (переменные `--s-gap-*`, рантайм-
  сторож `guardContrast` в `js-hero-fit2.js`) — это не дефект, а сделанная работа.

### 2.4 Мобильная адаптация — 6

- Брейкпоинтов **10 значений, 35 блоков**: 480/540/600/601/640/768/769/900/1100/1101/1280.
  Пары 600/601 и 768/769 — следы правок «на 1 пиксель», они гарантированно дают
  разные результаты на устройствах с дробным DPR.
- **`--header-height: 96px` не адаптивен** (`gtc.css:30`, переопределение
  `:1776-1780`) — при смене высоты шапки на мобильном контент уезжает под неё.
- Бургер 36×36 (`gtc.css:562-575`), крестик 36×36 (`:623-638`), `.mn-toggle` ≈32
  (`:3781-3799`), `.mn-sub a` ≈34 с `!important` (`:3814-3820`). WCAG 2.5.8 /
  Apple HIG требуют 44 px. На сайте это уже сделано для сенсорных целей в
  `gtc-dark.css:620` — но не для этих четырёх элементов.
- **Меню скрывается только `transform`-ом** (`gtc.css:594`), без `visibility` /
  `inert` — ≈13 ссылок закрытого мобильного меню остаются в порядке таб-фокуса.
- `#hero-si` 15 px (`gtc-dark.css:620`) → iOS принудительно зумит поле ввода.
- **`<main>` есть на 2 страницах из 171.** Неименованная кнопка — `mobile-nav-close`
  (`_partials/mobile-nav.html:2`, внутри только SVG); подозрение на
  `hero-search-btn` снято: у неё есть видимая подпись `{{search_btn}}`
  (см. журнал самопроверки, §0). Отсюда «Agentic browsing 67» при A11y 93 /
  BP 100 / SEO 100 в Lighthouse.
- **Иконки сайта отсутствуют полностью.** Все пути дают 404: `/favicon.ico`,
  `/favicons/*`, `/favicon-16x16.png`, `/site.webmanifest`, `/browserconfig.xml`,
  `/apple-touch-icon-180x180.png`, `/safari-pinned-tab.svg`. Каталога `favicons/`
  в репозитории нет; `rel="icon"` встречается только в чужих `gp.html`/`wc.html`.
  Плюс 404 на `/images/og-cover.jpg` (и на `/manifest.json`), при том что
  `shared.js:69-118` подставляет именно этот несуществующий `og-cover.jpg`.

---

## 3. КОНТУР B — блог

### 3.1 Организация — 8

112 статей + 3 хаба, ×3 языка = 115 файлов в трёх деревьях, 8 серий. Собрано из
**одного шаблона**, и это видно по метрикам: `canonical` / `hreflang` / `og:image`
/ `description` стоят ровно на 164 из 167 страниц сайта (в блоге — на всех 115), структура
блоков, CTA и привязка к sitemap зеркальны во всех трёх языках. Лучшая сборка из
трёх контуров.

Единственная организационная дыра — **`blog-klinik-psixiatriya.html` сирота**:
0 входящих ссылок, единственный файл без cover-изображения и единственный без CTA
на книгу (только `wa.me` на `:306`). При этом это хаб, который должен вести в книгу.

### 3.2 Завершённость — 8

- **Навигации по серии нет ни у одной статьи**: prev/next и «часть N/M» — 0 из 112.
  Серия из 4-8 частей читается как набор несвязанных страниц, это прямая потеря
  глубины просмотра.
- `reading-time` завышен примерно в 2.4 раза (считается по объёму HTML, а не по
  видимому тексту).
- **34 из 38 `<img>` в хабах с пустым `alt`**.
- Микроразметки типов контента нет: `Article` / `BlogPosting` — 0, дат publication
  / modification — 0, `og:type` = `website` и `og:image` = портрет врача на 115 из
  115 страниц. Google не видит здесь статей.

### 3.3 Стиль — 7.5

Нарушения терминологического канона (AZ), 7 мест:

| файл | строки | что |
|---|---|---|
| `blog-klinik-psixiatriya.html` | `:238, :283` | `mənzərə` (канон: `Klinik təzahürlər`) |
| `blog-klinik-psixiatriya.html` | `:231` | русское «oглавление» в AZ-тексте |
| `blog-sosial-fobiya-3.html` | `:6, :8, :32` | `üsulları` в meta description (канон: `metodları`) |
| `blog-depressiya.html` | `:199` | `xəstə` (канон: `pasiyent`) |
| `blog-klinik-psixiatriya.html` | `:236` | `xəstə` |
| `blog-depressiya-protokol.html` | `:237` vs `:211, :253` | `pasient` / `pasiyent` в одном файле, 7 : 13 |
| `blog-depressiya-protokol.html:203,301,304`, `blog.html:219` | — | опечатка `longituidnal` ×4 |
| `ru/blog.html` | `:170-175, :659` | `filterБлог` — кириллица в атрибуте-идентификаторе |

Пустой фильтр `ozunu` (`blog.html:189`, `ru:174`, `en:172`) — фильтр, который
ничего не фильтрует.

### 3.4 Мобильная адаптация — 5 (самая низкая оценка блога)

- **`blog-aile-usaq-2.html:269`** — inline `grid-template-columns:repeat(3,1fr)`
  на трёхколоночном related-гриде. Адаптивный класс `.blog-grid-2` существует
  **только в inline `<style>` трёх хабов** (`blog.html:19-21`) и отсутствует
  в `gtc.css` (0 совпадений). Итог на телефоне: карточки по **105 px**.
- **`.art-table` не имеет ни одного CSS-правила** (`blog-depressiya-protokol.html:208,
  :279, :303`; `blog-enurez-protokol.html:238`), а `gtc.css:3607` ставит
  `display:block` на `<table>` без обёртки `.tbl-wrap` → таблицы протоколов
  распадаются на мобильном.
  Примечание: в дереве `en/` тот же грид имеет корректный `grid-template-columns:1fr`
  и класс `.blog-grid-2` — то есть баг не шаблонный, а локальный, и в AZ/RU-дереве
  он уехал из-за inline-стиля.

---

## 4. КОНТУР C — электронная книга «Klinik Psixiatriya»

### 4.1 Организация — 8.5

4 дерева × 161 файл = 644. Разложение на 161 **одинаковое во всех четырёх
деревьях** (проверено): 104 карточки расстройств + 23 страницы глав (медиана 1906
слов, реальный текст) + 24 meta-refresh-заглушки + 10 служебных
(`abbreviatur`, `elave-acde`, `elave-skalalar`, `index`, `kitab-haqqinda`,
`melumat`, `mugeddime`, `mundericat`, `terminoloji-luget`, `yekun`). 548 из 708 URL
`sitemap.xml` — книга.

`<html lang>` корректен **161/161 на всех четырёх деревьях**. JSON-LD, `<main>` и
`description` стоят ровно на 137 реальных страницах; 24 заглушки вне sitemap —
это осознанное решение, а не пропуск.

**Сиротство `abbreviatur.html` и `terminoloji-luget.html` и отсутствие `<abbr>`**
трактуется здесь как **сознательное решение владельца** (коммит `b381970`), а не
дефект: виджет правок и вся его обвязка сняты 2026-08-09/10.

### 4.2 Завершённость — 9 (лучший показатель в репозитории)

| ось | факт |
|---|---|
| полнота разделов | **104/104** карточки держат 11 стандартных разделов (см. оговорку про 6A25) |
| паритет объёма | RU 1.03 · EN 1.03 · TR 0.99 к AZ — переводы не «усохли» |
| объём AZ | 397 679 слов, медиана карточки 2563 слова |
| библиография | 897 записей, **медиана 6**, max 57, **0 расхождений между четырьмя деревьями** |
| слабое место | **18 карточек с <5 записями**, у 8 карточек по 3 (6C70, 6C71, 6D11, 6D30, 6D34, 6E63, 7A21, 7A60) |

**6A25** (шизотипальное расстройство) — единственная карточка с альтернативным
планом: вместо 11 разделов 6 доменов симтомов (`3.1 … 3.6`) + `7. Mənbələr`,
одна карточка с нумерацией «7. Mənbə» вместо §11. 4256 слов, `ref-list` на месте.
Вопрос не «доделать», а **сознательно ли это исключение** — если нет, это сбой
канона; если да, правило в `checkup.py` надо на него настроить.

Формулировки разделов разъезжаются: **§6.1 в 8 вариантах** (69 мест вообще без
номера), **§7.1 в 5 вариантах**. Заголовки-нарушители канона: «Alətlər»,
«Alətlərə dair kritik xəbərdarlıq» (канон: `İnstrumental müayinələr`), `üsul*`
в H2/H3 (канон: `metodları`), `alət` в прозе `6C51.html`, два авторских `xəstə`
в `yekun.html`.

### 4.3 Стиль — 7

- `style.css:4-23` — **`--font` и `--mono` оба равны Montserrat**. Моноширинный
  шрифт, будучи пропорциональным, ломает выравнивание шкал и кодов в таблицах.
- **97 захардкоженных `font-size: …px`** вместо rem-шкалы → системный масштаб
  текста и браузерный зум обходятся неравномерно.
- `style.css:2` — `@import` для Montserrat: блокирующий каскад, см. CLS ниже.

### 4.4 Мобильная адаптация — 4.5 (худшее в репозитории)

Замерено на проде `/klinik-psixiatriya/6B20.html`, 390×844:

1. **CLS 0.10** — порог «хорошо» 0.10 формально на границе, но причина именно
   наша: подмена Montserrat из-за `@import` (`style.css:2`). Чинится self-hosted
   шрифтом + `font-display` и, попутно, несёт LCP.
2. **LCP 365 мс, из них 361 мс — render delay.** Контент не грузится долго,
   он не отрисовывается: `@import` + блокирующий CSS.
3. **Контент 319 px при viewport 407**, паддинги 44/44. То есть 22 % ширины
   телефона съедено полями.
4. **Сайдбар: 341 ссылка, 55 кнопок `.nav-toggle`, 316 под-ссылок свёрнуты** — а
   сами кнопки раскрытия **20×17 px**. Это не «тесновато», этим невозможно
   попасть пальцем; `style.css:486-498`.
5. Off-canvas объявлен на `@media (max-width: 960px)` (`style.css:409-424`),
   `.sidebar` — `fixed` 280 px (`style.css:216-220`): на 390 px панель занимает
   72 % экрана.
6. **Поиск ломается на нелатинице.** `depresiy` → 0 результатов, `sizofren` →
   0. Корень — `toLowerCase()` без Unicode-normalize: «İ» разваливается на
   U+0069 + U+0307, и индекс с текстом перестают совпадать. В книге **129 из 1397
   заголовков содержат İ/I**. Вторичный дефект — ранжирование (совпадение в
   середине слова равно совпадению в начале).
7. `style.css:628-631` — `page-break` для печати: **это правильно, не трогать.**

---

## 5. Реестр дефектов по приоритетам

### P0 — теряет деньги или публикует чужое (чинить сейчас)

| # | дефект | место | следствие |
|---|---|---|---|
| 0.1 | Форма B2B не отправляет ничего | `b2b.html:371-378`, `ru/b2b.html:356-363`, `en/b2b.html:356-363` | 100 % B2B-лидов потеряно, пользователь видит «Göndərildi» |
| 0.2 | Чужие скопированные страницы в проде | `gp.html`, `wc.html` (tracked, HTTP 200) | чужая репутация, чужой GA/New Relic исполняется на нашем домене |
| 0.3 | Модалка заказа книги всегда «успешна» — **на стороне сайта** | `_partials/kitab-modal.html:52-56` (`mode:'no-cors'`, success в `catch`), 167 страниц | на сайте канал отсутствовал как класс: ответ не читался, ошибка не показывалась. **В дереве книги (548 страниц) копия иная и лучше**: заявка идёт на `api.ragimoff.org/api/book-order` плюс резервный WhatsApp-переход — но результат тоже не проверяется, поэтому это P1 (молча проглоченный сбой), а не P0. См. §0, строка 7 |
| 0.4 | На редирект-странице входа в книгу нет `viewport` и `description` | `klinik-psixiatriya.html:4-5` | страница из sitemap и с внешних ссылок не читается на телефоне (изначально записано как «пустая витрина» — снято, см. §0) |

### P1 — ломает найденное и измеряемое

| # | дефект | место |
|---|---|---|
| 1.1 | Иконки/манифест отсутствуют, `og-cover.jpg` 404 | все пути 404; `shared.js:69-118` подставляет несуществующий файл |
| 1.2 | JSON-LD только в AZ (`56/167`), `ru`/`en` — 0 | дерево `ru/`, `en/` |
| 1.3 | `<main>` на 2 из 171; неименованные кнопки | `_partials/mobile-nav.html:2`, `_partials/hero-search.html:4` |
| 1.4 | 3 ссылки footer в `tehsil.html`, дубли соцсетей, нет `rel="noopener"` | `_partials/footer.html:33-35`, `:12-15`, `:44-49` |
| 1.5 | `samira.html` вне навигации и sitemap | `_partials/header.html`, `sitemap.xml` |
| 1.6 | Поиск RU/EN ищет по AZ-индексу; корневой `search-index.json` осиротел (559 881 Б от 2026-05-14) | `shared.js:681,694`, `build_search_index.py:35-36` |
| 1.7 | Нет prev/next и «часть N/M» — 0 из 112 | весь блог |
| 1.8 | `Article`/`BlogPosting`/даты — 0, `og:type=website`, 34/38 пустых `alt` | весь блог |
| 1.9 | Поиск книги не находит `depresiy`/`sizofren` (case-folding) | `klinik-psixiatriya/*/search*.js` |
| 1.10 | Мобильный сайдбар книги: кнопки 20×17, 72 % ширины, CLS 0.10, render delay 361 мс | `style.css:2`, `:216-220`, `:409-424`, `:486-498` |
| 1.11 | Грид related-статей 3 колонки на телефоне (105 px) | `blog-aile-usaq-2.html:269` |
| 1.12 | `.art-table` без правил, `display:block` на таблицах без обёртки | `blog-depressiya-protokol.html:208,279,303`; `blog-enurez-protokol.html:238`; `gtc.css:3607` |
| 1.13 | **Авторское решение от 2026-08-25 не доехало до единого источника шапок.** `PROGRESS.md:1203` фиксирует коммит `d174f14`: «6E20/6E21 разделены различителем „без психоза/с психозом"». Реализовано это было **приставкой** в шапке страницы (`Psixozsuz —`, `Без психоза —`, `Without Psychosis —`, `Psikozsuz —` и парной для 6E21), а в `_codes_canon.json → header_source.rows` такой приставки нет. Поэтому `build_headers.py` считает её мусором и при каждом `--apply` стирает на 8 страницах (6E20/6E21 × 4 дерева). ⚠️ **Клинической ошибки здесь нет**: различие рубрик живёт в хвосте официального имени XBT-11 (`…PSİXOTİK SİMPTOMLARSIZ` против `…PSİXOTİK SİMPTOMLARLA`), оно в каноне есть и никуда не денется. Теряется только дублирующий префикс, то есть шапка возвращается к виду «официальное имя без пояснения». Правильное решение — не спорить с генератором, а **внести префикс в `header_source`** (или сознательно принять голый официальный титул). | `_codes_canon.json` (`header_source.rows`), `build_headers.py`, `klinik-psixiatriya/{,ru,en,tr}/6E20.html`, `/6E21.html` |
| 1.14 | **Два источника имён разделов расходятся по термину.** `_build_abbreviatur.py:20,71` (обе формы вшиты прямо в скрипт — `_codes_canon.json` он не читает вообще) + `_codes_map.json` + `_en.json` + `TYPOGRAPHY.md:35,39,51` + `PROGRESS_MAP.md` несут `NARAHATLIQ POZUNTULARI` / `ADAPTASİYA POZUNTUSU`; изданные карточки и `_codes_canon.json:39-43` — блок `az_anxiety_verdict` (вердикт с обоснованием: в азербайджанских клинических протоколах «anxiety» = **TƏŞVİŞ**, а NARAHATLIQ там означает «волнение/беспокойство», то есть компонент GAD, и ANKSİYETE — турецкая форма) несут `TƏŞVİŞ POZUNTULARI` / `UYĞUNLAŞMA POZUNTUSU`. Вхождения измерены раздельно — как название и как голое слово (текст без тегов, верхний регистр, все четыре дерева):
`NARAHATLIQ POZUNTULARI` — **0**, `TƏŞVİŞ POZUNTULARI` — 288 (все — в аз. дереве); `UYĞUNLAŞMA POZUNTUSU` — 442 (436 в аз. и по 2 в ru/en/tr, где это цитата азербайджанского термина), `ADAPTASİYA POZUNTUSU` — **0**.
Стержень `narahatl-` живёт в книге 314 раз (все четыре дерева), но **везде это строчное описание симптома** («daxili narahatlıq»,
«komorbid narahatlıq», «həddən artıq narahatlıq») — ровно то, что коммит `33b6456` и оставлял: бытовое слово не трогалось. То есть как **название диагноза** старая форма в книге мертва. Само собой не рассосётся и уже применялось частями: в `PROGRESS_MAP.md` строки 6B00/6B04/6B43 — `NARAHATLIQ`, а 6E63 — `TƏŞVİŞ`, то есть карта противоречива внутри себя. Оговорка: `TYPOGRAPHY.md:104` держит `İKİNCİLİ NARAHATLIQ SİNDROMU` для **6E62** — это другая рубрика, под решение по 6B00/6B04/6B05 она не попадает и заменой по слову её трогать нельзя | `_build_abbreviatur.py`, `klinik-psixiatriya/abbreviatur.html` |
| 1.15 | `build_sections.py` хочет переименовать 5-й раздел `6A25` («Mənbələr arasında fərq» → «Mənbə-spesifik…») в az/en/tr — 3 страницы; это та же карточка с альтернативным планом из §0 | `build_sections.py`, `klinik-psixiatriya/{,en,tr}/6A25.html` |
| 1.16 | **Подписи нижнего навигатора `d-nav` разошлись с заголовками карточек — 23 подписи, и ни один сторож этого не видит.** Замер: 644 подписи `d-nav` по четырём деревьям, из них 23 не являются префиксом собственного `H1` целевой карточки. Делятся на три разных дефекта: **(а) не докатанное переименование владельца** — `az/6B00` `GENERALİZƏ OLUNMUŞ NAR…`, `az/6B04` `SOSİAL NARAHATLIQ POZU`, `az/6B05` `AYRILMA NARAHATLIĞI PO` при новых заголовках `TƏŞVİŞ` (коммит `33b6456` от 29.08 правил `H1`, `<title>`, `og:title`, «хлебную крошку» и боковое меню — 136 файлов, — но не `d-nav`); в 6B05 хвост вообще турецкий (`-LIĞI`, а не азер. `-LIĞI/‑LIQ`), то есть остаток из ранней версии; **(б) потеря того же авторского различителя, что и в п. 1.13** — 8 подписей `6E20/6E21` (4 дерева × 2) идут без приставки `Psixozsuz/Без психоза/Without Psychosis/Psikozsuz`, что независимо подтверждает: решение живёт в одном-единственном месте книги; **(в) расхождение по сути названия** — `az/6A03` (`SPESİFİK ÖYRƏNMƏ` vs `İNKİŞAF ÖYRƏNMƏ`), `az/6A04`, `az/6C73` (`ARALIQ PARTLAYICI` vs `EPİZODİK EKSPLOSİV`), `az/6C91` (`DAVRANIŞ POZUNTUSU` vs `DAVRANIŞDİSSOSİAL POZUNTU` — а 6C90/6C91 были переставлены местами, так что это единственный случай, где устаревшая подпись может значить не стиль, а перепутанный код), `tr/6D50`, `tr/6D51`, `ru/6B41` (`ОСЛОЖНЁННОЕ ПТСР` vs `КОМПЛЕКСНОЕ ПТСР` — разные сущности), плюс `ru/6E20/6E21` «И» вместо «ИЛИ» в официальном титуле. ⚠️ Ни `xrefcheck.py` (он смотрит, что ссылка ведёт в существующий файл), ни `checkup.py`, ни `regress.py` содержимое `dn-name` не сравнивают — **слепая зона сторожей**, предлагаю отдельный `dnavcheck.py` по образцу `xrefcheck.py` | `klinik-psixiatriya/*/….html` (блок `<nav class="d-nav">`, `<span class="dn-name">`), генератор — `build_book.py` (он же `МЁРТВ`-соседей имеет: `fix_terminoloji.py`, `build_terminoloji.py` — путь `sayt2`) |

### P1-Б — ритуал старта сессии сейчас невыполним (замерено 2026-09-02)

`CLAUDE.md` требует, чтобы генераторы показали 0 изменений. Фактически в
неизменённой книге (я её в волне 0 не трогал) они показывают:

| генератор | пробный прогон | вердикт |
|---|---|---|
| `checkup.py` | 0 сбоев из 19 проверок | чисто |
| `refcheck.py` | 0 пропусков, 0 имён | чисто |
| `numcheck.py` | 0 расхождений из 312 (104 × 4 − 104 az-мастер) | чисто, заодно подтверждает 104 карточки |
| `paracheck.py` | одинаковых блоков 0, пустых 0; 4 информационных строки про длину абзацев в `6A05` en | чисто |
| `xrefcheck.py` | 0 висящих ссылок; 6 кодов XBT-11 без своей карточки (`6A41, 6C48, 6C4C, 6C4D, 6C4G, 6E69`) — информационно | чисто |
| `fix_orthography.py` | 0 замен | чисто |
| `lang_tags.py` | 0 правок на 4 дерева | чисто (см. оговорку про stderr) |
| `fix_sidebar.py`, `fix_toc_codes.py` | 0 страниц, 0 кодов | чисто |
| `_build_abbreviatur.py` | **перезаписывает файл всегда** и приносит два термина из п. 1.14 + убирает 3 пустые строки | идемпотентность нарушена |
| `build_headers.py` | **11 из 416** (6E20/6E21 × 4 + `az/6B84`, `az/6C46`, `tr/6D33`) | требует решения владельца |
| `build_sections.py` | **3 заголовка** (`6A25` az/en/tr) | требует решения владельца |

**Как это было измерено честно.** Пробный прогон без `--apply` строки длиннее
96 символов не расшифровывает (об этом — ниже, п. P2). Поэтому генераторам
разрешили писать, но **не в этом дереве**: был создан одноразовый
`git worktree` на коммите `33b6456` (`D:\Документы\_driftwt_20260902`), там запущены
`build_headers.py --apply` и `build_sections.py --apply`, и расхождение снято
обычным `git diff` внутри копии. Живое дерево при этом не тронуто ни на байт,
а после замера копия удалена — проверено `git worktree list`: осталась только
`D:\Документы\ragimoff`. Итог: **14 файлов, 17 замен** (17 удалённых и 17
добавленных строк). Цифра менялась дважды и оба раза из-за ошибки измерителя, а
не из-за генератора: сначала «14» было снято с обрезанного консольного `--stat`,
затем «13» — потому что копия диффа, сохранённая через `Out-File -Encoding utf8`,
начинается с BOM, и `^diff --git` не увидел **первый** файл списка. Перечитывание
копии как `utf-8-sig` даёт 14 заголовков при тех же 17 строках — это и есть
правильный ответ, сошлённый и по файлам, и по строкам. Раскладка по позициям:
`6E20`/`6E21` × 4 дерева = 8 файлов по 1 строке, `6A25` az + en + tr = 3 файла
по 2 строки (пункт оглавления и `h2`), плюс `az/6B84`, `az/6C46`, `tr/6D33` — по
1 строке. 8 + 3 + 3 = 14 файлов; 8 + 6 + 3 = 17 замен.

Расшифровка по позициям, теперь полная — и она **не в пользу генераторов**:

| строк | файл | что предлагает генератор | вердикт по факту |
|---|---|---|---|
| 8 | `6E20`/`6E21` × 4 дерева | снять приставку-различитель (`Psixozsuz —`, `Без психоза —`, `Without Psychosis —`, `Psikozsuz —`) и вернуть канонный порядок слов | **не клинический дефект.** Различие рубрик канон держит в хвосте официального имени (`…PSİXOTİK SİMPTOMLARSIZ` против `…PSİXOTİK SİMPTOMLARLA`), оно никуда не денется. Спорный только **порядок**: страница ставит различитель в начало, канон — в конец. Различим при этом и `d-nav` нельзя — там обрезано и начало, и конец (п. 1.16) |
| 1 | `az/6C46` | `METKATİNON` → `MEKATİNON` в title и H1 | **не опечатка карточки, а системный разнобой деревьев.** Аз. дерево: `MEKATİNON` 412, `METKATİNON` 5 (все пять — на этой карточке). Турецкое дерево: `METKATİNON` 417, `MEKATİNON` 0. При этом в самом каноне `title.ru` = «…ИЛИ МЕТКАТИНОН», `title.en` = `methcathinone`. То есть применение согласует карточку с аз. корпусом, но аз. корпус расходится с турецким и с МНН. Вопрос владельцу (P1), а не «генератор прав» |
| 1 | `az/6B84` | `körpələrin` → `körpələrni` в строке XBT-10 | **опечатка в каноне.** `körpələrni` по всей книге (4 дерева, текст без тегов) — **0** вхождений; `körpələrin` — 1, ровно на этой странице. `--apply` внёс бы дефект в издание |
| 1 | `tr/6D33` | `Cinsel Sadizm Bozukluğu` → `Cinsel Elezerlik (Sadizm) Bozukluğu` в строке DSM | **опечатка/инородная вставка в каноне.** `Elezerlik` во всей книге — **0** вхождений, `SADİZM` — 420. Канонное значение уникально и ничем не подтверждено |
| 6 | `6A25` az/en/tr (по 2 строки: пункт оглавления и `h2`) | переименовать 5-й раздел: `Mənbələr arasında fərqlər` → `Mənbə-spesifik dəqiqləşdirmələr`, `Differences between sources` → `Source-specific specifications`, `Kaynaklar arasındaki farklar` → `Kaynağa özgü belirlemeler` | **по сути полезно:** `ru`-страница уже несёт канонную формулировку («Источник-специфические уточнения»), то есть решение принято, а три остальных дерева отстали. Az-карточка в диффе **есть** — прежний вывод о её пропуске снят как следствие BOM-ловушки выше |

**Ложная тревога, которую эта сессия всё-таки зарегистрировала.** Сначала
здесь стояло, что `--apply` записал en и tr, а аз. карточку «молча пропустил».
Это не так: `az/6A25` оказалась **первым** файлом в спасённой копии диффа, и
именно её заголовок съел BOM. После перечитывания в `utf-8-sig` аз. замены
видны (оглавление + `h2`), то есть генератор отработал по всем трём деревьям, и
никакого скрытого пропуска нет. Правило «после `--apply` — повторный пробный
прогон» остаётся как гигиена ритуала, но уже не как следствие дефекта генератора.

**Что осталось в истории и не поправимо.** Сообщение коммита `a7eaa8c` (волна 0)
несёт снятую цифру «13 файлов» и снятую «аномалию» `az/6A25`, а в тексте той же
сессии проскочило и неверное имя канала отправки. `CLAUDE.md` запрещает amend,
поэтому поправка живёт здесь, в `TASKS.md` 1.13–1.14 и в коммите регистрации, а
не в теле `a7eaa8c`. Кто читает `git log` — обязан знать об этом.

То есть из 17 pending-замен **6 несут пользу (`6A25` в трёх деревьях), 8 меняют
только порядок слов (`6E20`/`6E21`), а 3 — это сам канон: `az/6B84` и `tr/6D33`
вносят в издание прямые опечатки, `az/6C46` согласует карточку с аз. корпусом
вопреки МНН**. Вывод переносится в гейт:
`--apply` у `build_headers.py` допустим только после того, как `az/6B84` и
`tr/6D33` исправлены **в `_codes_canon.json`**, а не в страницах; страницы —
производные, канон первичен.

**Инцидент этой сессии, который будущий агент прочтёт здесь же.** Часть первых
замеров (в том числе вывод «в каноне 104 кода с готовыми шапками» и часть
терминологической сверки) снималась не с отслеживаемых файлов, а с неотслеживаемой
папки-копии книжной подсистемы, которая в начале сессии лежала в
`D:\Документы\ragimoff\Klinik Psixiatriya` и содержала СВОЙ `_codes_canon.json`
на 868 566 байт (втрое больше репозиторного, с другой схемой: коды лежали
плоскими ключами `header_source`, а не в `header_source.rows`). К концу сессии
папка исчезла: `git ls-tree HEAD` её не знает, `git status` удалений не показывает
(не отслеживалась), в корзине её нет, `Get-ChildItem -Recurse -Force` по всему
репозиторию находит единственный `_codes_canon.json` — корневой, 257 056 байт.
Кто и что с ней сделал — не установлено; HEAD с 29.08 не двигался, параллельной
агентской работы в репозитории нет. **Все числа в этом отчёте после находки
пересняты заново, строго по отслеживаемым файлам** (`_codes_canon.json` в корне +
`klinik-psixiatriya/{,ru,en,tr}`), а там, где старый замер оказался неверным, в
тексте оставлена прямая пометка о пересчёте: например, вердикт по `az/6C46`
перевернулся (см. п. 1.16), а счёт подписей `d-nav` стал 644 вместо 636. Практическое правило отсюда:
любой замер книги надо привязывать к путям из `git ls-files`, иначе результат
может оказаться снят с чужой копии.

**Побочный дефект наблюдаемости (P2):** и `build_headers.py`, и
`build_sections.py` печатают пробный прогон с жёстким усечением строк. Для
коротких шапок это безопасно, но именно из-за усечения первый вывод об
«исчезновении различия 6E20/6E21» выглядел правдоподобно и был бы записан в
реестр как клинический дефект. Предложение: в пробном режиме печатать длину
строки и полный хвост, либо давать `--full`.

**Практический вывод для следующей сессии:** запускать `--apply` у этих трёх
генераторов нельзя до терминологического решения владельца. Отсюда —
предложение в `CLAUDE.md`: ритуал старта должен сравнивать вывод генераторов не
с нулём, а с сохранённым ожидаемым списком (`11 + 3 + abbreviatur`), иначе
агент, честно выполняя «покажи 0 изменений», тихо портит две рубрики XBT-11.

### Оговорка к замерам: ложное срабатывание обёртки

`lang_tags.py` в моём прогоне выглядел упавшим («ИСКЛЮЧЕНИЕ: SyntaxWarning:
invalid escape sequence '\s'»). Причина — не скрипт: PowerShell при
`$ErrorActionPreference='Stop'` превращает любой stderr-вывод нативной команды
в ошибку. Сам скрипт идемпотентен (0 правок). Но предупреждение настоящее
(`lang_tags.py:284-286` — `\s` в не-raw строке): сегодня Python трактует его как
литеральный `\s`, то есть поведение совпадает, а с перехходом на новую версию
это станет ошибкой. Исправлено в волне 0 (`r"..."`), проверено под
`-W error::SyntaxWarning`: выход 0, правок 0.



терминологические нарушения блога (7 мест) и книги (`alət`/`üsul*`/`xəstə`);
`longituidnal` ×4; `filterБлог`; пустой фильтр `ozunu`; §6.1 в 8 формулировках,
§7.1 в 5; 97 inline `font-size: px` в книге; `--mono` == `--font`;
18 карточек с <5 источников; `reading-time` завышен 2.4×; статус `.tbl-wrap`
в книге (`style.css:797-819`) не перенесён в блог.

### P3 — структурный долг

5511 инлайн-стилей на сайте; 339 `!important` в `gtc-dark.css`; 10 брейкпоинтов /
35 блоков / 3 пары «на пиксель»; 8 мёртвых генераторов в `backend/`; устаревшие
поля `PROJECTS.json`.

### Вне рамок вёрстки — решение только владельца

В репозитории опубликованы **14 DOCX + `basliqlar_resmi_yoxlama.xlsx`,
`terminologiya_tezlik.xlsx`, `XBT10_az_quality_check.docx`**. Это вопрос
информационной безопасности и авторских прав (рабочие файлы, черновики,
проверочные таблицы уходят на публичный GitHub Pages), а не «мусор в репо».
Агент не удаляет их самостоятельно.

**Терминологический выбор (п. 1.14) — только владелец.** Две пары форм живут в
репозитории одновременно и обе опираются на авторитетный источник:
`TƏŞVİŞ POZUNTULARI` / `UYĞUNLAŞMA POZUNTUSU` — 288 и 442 заголовка изданных
карточек, плюс вердикт `_codes_canon.json:39-43` (блок `az_anxiety_verdict`) — против
`NARAHATLIQ POZUNTULARI` / `ADAPTASİYA POZUNTUSU` в `_codes_map.json`,
`_en.json`, `TYPOGRAPHY.md` и данных `_build_abbreviatur.py`. Пока решения нет,
`abbreviatur.html` не перегенерируют (в волне 0 его изменение откатано).

**`--apply` у `build_headers.py`, `build_sections.py`, `_build_abbreviatur.py` —
отдельный гейт.** Один честный прогон ритуала старта снимает приставку-различитель у
двух рубрик XBT-11 (п. 1.13; клинического различия при этом нет —
оно живёт в хвосте официального имени XBT-11), **вносит две опечатки прямо
из канона** (`az/6B84`, `tr/6D33`; п. 1.16), оставляет неразобранным
разнобой `6C46` между аз. и турецким деревьями и меняет два термина
(п. 1.14).

---

## 6. План исправлений — логичный и последовательный

Принцип порядка: сначала то, что **теряет деньги и публикует чужое**; потом то,
что **ломает найденное** (SEO/навигация/поиск); потом **качество текста**; долг —
последним, потому что он требует согласованной системы токенов, а не точечных
правок. Каждая волна заканчивается прогоном сторожей и отдельным коммитом.

**Порядок волн (обязателен перед каждой):**
`git tag backup-before-audit-<волна>-<дата>` → правки → `build.py` →
`checkup.py` → `refcheck.py` → `numcheck.py` → `paracheck.py` → `xrefcheck.py` →
`build_headers.py` / `build_sections.py` / `fix_orthography.py` / `lang_tags.py`.
Формулировка `CLAUDE.md` «все должны показать 0 изменений» на книге 2026-09-02
**невыполнима**: `build_headers` даёт 11 расхождений, `build_sections` — 3,
`_build_abbreviatur.py` всегда перезаписывает файл. Значит ритуал надо читать
так: сверять вывод с ожидаемым списком п. 1.13–1.15 и не пускать `--apply`,
пока владелец не решил термины. `regress.py` (25–40 мин) — **только
с явного разрешения**.

### Волна 0 — «кровь и мусор» (P0, без вкусовых решений)
1. Подключить `handleB2BForm` к существующему `submitRegistration`-каналу
   (`shared.js:250-267`) с фолбэком и **честной** веткой ошибки (`shared.js:271-294`)
   — три файла.
2. Убрать из публикации `gp.html`, `wc.html` (git rm) — вместе с их чужой
   аналитикой.
3. Починить `kitab-modal`: `no-cors` → читаемый ответ, success только из ветки
   успеха, добавить `submit`-локировку. Правка в партиале → `build.py`, и она
   разойдётся по ~170 страницам сама.
4. `viewport` + `description` на `klinik-psixiatriya.html` (это редирект-страница,
   контент ей не нужен).
5. Быстрые a11y-винды: `aria-label` кнопке закрытия мобильного меню (вторая
   кнопка, `hero-search-btn`, оказалась ложноположительной — см. §0).

**Итог волны 0 (выполнено в этой же сессии).** Пункты 1–5 закрыты; пункт про
548 копий модалки книги сознательно перенесён (см. ниже) как 0.6/P1.
Реализация: `handleB2BForm` в `shared.js` (+60 строк) читает поля по `id`
(у инпутов нет `name`, поэтому штатный `wireRegForm` не подходил), несёт
`type:'registration'` — формат payload не менялся, потому что приёмники
(Apps Script и Flask) вне репозитория, а различие потока передаётся полями
`source` и префиксом `B2B | …` в `note`; надписи кнопки «отправляем/готово»
вынесены в `data-sending`/`data-done` самих страниц, чтобы логика осталась в
общем `shared.js`. `kitab-modal.html` переведён на тот же канал, ветка
`!result.ok` возвращает подпись кнопки и зовёт `showSendError`, форму не прячет.
`gp.html`/`wc.html` сняты **неразрушающе** (`git rm --cached` + блок в
`.gitignore`): файлы остались на диске, ссылок на них в репозитории нет, значит
висящих путей не появилось. Проверки после `build.py`: повторный `--check` = 0
из 206 (неподвижная точка); состав diff объясним целиком — 163 страницы получили
прирост строк из-за разрастания `kitab-modal` (+18) и `mobile-nav` (1 строка),
а не скрытый дрейф; висящих `onclick`-обработчиков на 213 страницах — 0;
ни одного определения не потеряно. `index.html` (эталон) в диффе отсутствует —
не тронут.

### Волна 1 — сайт и блог (P1, через партиалы и генераторы)
6. `favicon.ico` + `apple-touch-icon` + `/favicons/` + `site.webmanifest` +
   `browserconfig.xml`, `rel="icon"` в `_partials/header.html`; сгенерировать
   из существующего логотипа, не выдумывая бренд. Заодно либо положить реальный
   `images/og-cover.jpg`, либо убрать его подстановку в `shared.js:69-118`.
7. `<main>` на страницы сайта — через обёртку в `_partials/`, а не ручной правкой
   171 файла.
8. Footer: 3 ссылки на программы вместо `tehsil.html`, убрать дубли Instagram/
   Telegram, добавить `rel="noopener"`.
9. `samira.html` — в `header.html` и `sitemap.xml`, добавить в `search-index.json`.
10. JSON-LD (`Article`/`BlogPosting`, `datePublished`/`dateModified`,
    `og:type=article`, `twitter:card`) — генератором, по образцу AZ-дерева.
11. Поиск: нормализация индекса и запроса (NFC + Turkish/AZ case-fold), затем
    перебороть расхождение RU/EN по AZ-индексу (`shared.js:681,694`) и regeneration
    корневого `search-index.json`.
12. Блог: prev/next и «часть N/M» (данные серий уже есть в разметке хабов),
    alt-тексты 34 изображений, убрать inline `grid-template-columns` из
    `blog-aile-usaq-2.html:269` в пользу общего класса, который перенести в
    `gtc.css`, правила `.art-table` + обёртки `.tbl-wrap` (перенести из книги).
13. Ветка `redesign/search-first-platform` и «4 версии в корне» — lifecycle-решение
    владельца, не техническая правка.

### Волна 2 — книга (P1/P2, мобильное чтение)
14. Поиск книги: NFC-normalize в индексаторе и в запросе, приоритет начала слова.
    Тесты: `depresiy`, `sizofren`, `İ`-содержащие 129 заголовков.
15. Мобильный сайдбар: hit-target ≥44 px, панель как overlay ≤ 85 % ширины,
    закрытие по свайпу/оверлею, состояние «развёрнуто» — не обязательное.
16. Шрифт: `@import` → self-hosted `woff2` + `font-display: swap`, `--mono` →
    настоящая моноширинная гарнитура, добить LCP/CLS (замерить повторно).
17. 97 `font-size: px` → rem-шкала; паддинги контента 44/44 → адаптивные.
18. Терминология: `alət`/`üsul*`/`xəstə` в заголовках и прозе, унификация §6.1 и
    §7.1 — **через генераторы** (`build_headers.py`, `build_sections.py`), не
    ручным поиском; решение по 6A25 (исключение или сбой) — владелец.
19. 18 карточек с <5 записями: пополнение требует сверки с PubMed/белым списком
    источников, автоматического добавления не будет.

### Волна 3 — долг (P3) и документация
20. Брейкпоинты → 3 именованных токена, убрать пары 600/601, 768/769, 1100/1101.
21. Ревизия `!important` в `gtc-dark.css`, сокращение инлайн-стилей через
    компоненты партиалов.
22. `backend/` — удалить или задокументировать как мёртвый; обновить
    `PROJECTS.json` (числа страниц, ссылки на удалённые объекты).
23. Решения по публикации DOCX/XLSX — по ответу владельца.

### Гейты, где агент обязан остановиться и спросить
- любой diff в `index.html` (эталон; правило `CLAUDE.md`) — в том числе
  `build.py --migrate`, который чинит 6 мест дрейфа;
- удаление файлов, которые могут быть нужны владельцу (DOCX/XLSX, `template.html`);
- запуск `regress.py`;
- `git push`;
- установка ПО/зависимостей;
- любой вкус: формулировки, цвета, «красиво ли».

---

## 7. Что проверено и дефектом НЕ является

Чтобы не «починить» сознательные решения:

- `page-break` в `style.css:628-631` — печать книги, оставлено намеренно.
- 24 meta-refresh-заглушки и отсутствие на них `viewport`/JSON-LD/`<main>` —
  они вне sitemap.
- Сиротство `abbreviatur.html` / `terminoloji-luget.html`, отсутствие `<abbr>`
  в тексте и снятый виджет «Düzəliş et» — коммит `b381970`, решение 2026-08-09/10.
- Разметка `lang`-этaпов A–E (`lang_tags.py`) — 208 + ~2170 + 454 + 387 + 909
  меток; фамилии авторов, аббревиатуры, международные МНН и латынь помечены
  не быть должны, и не помечены — это соответствует правилу, а не нарушает его.
- Орфографические пары из `fix_orthography.py` (`depressiv`/`depresif`,
  `apnoe`/`apne`, `-ergik`/`-erjik`, `DDHP`/`DEHB`) — решения владельца 2026-08-10,
  сводить к одной форме дальше не надо.
- Рантайм-сторож контраста в `js-hero-fit2.js` вместо CSS-правила — сделано
  сознательно (правила по `data-theme` промахивались).
- `graphify-out/` в `.gitignore` — так и задумано.

---

## 8. Сырые измерения (для повторного замера после исправлений)

```
РЕПОЗИТОРИЙ
  tracked файлов: 1133 | HEAD: 33b6456 (2026-08-29) | грязных: 1
  скриптов в корне: 74 (живых идемпотентных 25, мёртвых 41, опасных одноразовых 5)
  gtag: G-SF6PE3YDN1 | приёмник заявок: api.ragimoff.org (Flask :9631, с 28.08)

САЙТ
  html: 171 (root 60 / ru 56 / en 55) | инлайн style=: 5511
  og:image 164/167 · description 167/167 · canonical 167/167 · hreflang 164/167
  viewport 166/167 · JSON-LD 56/167 (az) / 0/55 (ru) / 0/55 (en) · twitter:card 0/167
  <main>: 2/171
  Lighthouse /ru/index.html: A11y 93 · BP 100 · SEO 100 · Agentic 67
  404: /favicon.ico /favicons/* /favicon-16x16.png /site.webmanifest /browserconfig.xml
       /apple-touch-icon-180x180.png /safari-pinned-tab.svg /images/og-cover.jpg /manifest.json
  200 + чужая аналитика: /gp.html /wc.html
  брейкпоинты: 10 значений / 35 блоков | gtc-dark.css: 339 !important / 243 правила

БЛОГ
  112 статей + 3 хаба, 8 серий, 115 файлов × 3 языка
  prev/next: 0/112 · Article/BlogPosting: 0 · даты: 0 · og:type=website: 115/115
  пустые alt в хабах: 34/38 · reading-time завышен ≈2.4×

КНИГА
  4 дерева × 161 = 644: карточки 104 · главы 23 · заглушки 24 · служебные 10
  <html lang> 161/161 на всех деревьях · JSON-LD/<main>/description ровно на 137
  карточки полные 11/11: 103/104 (+1 альтернативный план 6A25)
  объём AZ 397 679 слов · медиана карточки 2563 · RU/EN/TR = 1.03/1.03/0.99
  §11: 897 записей · медиана 6 · min 3 (8 карточек) · max 57 · <5: 18 · расхождений 0
  прод /klinik-psixiatriya/6B20.html @390×844:
    LCP 365 мс (render delay 361 мс) · CLS 0.10 · контент 319 px при 407 · паддинги 44/44
    сайдбар 341 ссылка / 55 nav-toggle / 316 sub-link · кнопка 20×17 px
    поиск: "depresiy" → 0 · "sizofren" → 0 · заголовков с İ/I: 129 из 1397
```

---

*Файл создан по итогам сессии 2026-09-02. Предшественник: `_AUDIT_2026-05-15.md`.
Состояние работ по этому плану: `GM.md` §«План исправлений» и `TASKS.md` §«Аудит 2026-09-02».*
