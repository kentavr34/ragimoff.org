# Handoff из MiniMax Code → ZCode (2026-09-08)

Сводка, извлечённая из локальных данных MiniMax (`C:\Users\KENAN\.minimax\`) и
из фактического состояния рабочей копии `D:\Документы\ragimoff` на 08.09.2026.
Цель — перенести «последние данные» из программы в папку проекта, как просил Кенан.

---

## 1. Доступы к GitHub — проверены, работают

- Remote: `https://github.com/kentavr34/ragimoff.org.git` (GitHub Pages из `main`, CNAME `ragimoff.org`).
- Учётные данные: в Windows Credential Manager лежит сохранённый логин
  `kentavr34` (пароль/токен на месте) — `git credential fill` возвращает его без запроса.
- `git ls-remote origin` проходит; локальный `main` = `origin/main` = `2b1b9df`
  — некоммиченных коммитов нет, всё запушенное на месте.
- `gh` CLI не установлен (не нужен для git push, но может пригодиться для проверки
  сборки Pages через API).
- Живой сайт отдаёт ровно последний коммит: `Last-Modified: 2026-09-05 18:54 UTC`
  (= 22:54 AZT, момент коммита `2b1b9df`); `llms.txt` → HTTP 200, JSON-LD на главной есть.

## 2. Почему последние изменения НЕ видны на сайте

**Все правки после коммита `2b1b9df` (вечер 5 сентября) так и не были закоммичены.**

- `git status`: **202 изменённых + 6 неотслеживаемых файлов**, 0 незапушенных коммитов.
- GitHub Pages публикует содержимое репозитория, а не рабочую папку — пока нет
  `git commit` + `git push`, сайт не меняется. Деплой-механизм сам по себе исправен
  (см. п.1: сайт = последний коммит).
- Правки лежат в рабочей копии с 6–7 сентября (массовая пакетная правка 06.09 ~03:14
  по 199 файлам; затем точечные правки gtc.css, tehsil.html, xidmetler.html, samira.html).

## 3. Почему работу над дизайном не довели до конца — диагноз

В ночь на 6 сентября по всему сайту прогнали пакетные скрипты (в корне остались
неотслеживаемыми: `standardize_az.py`, `standardize_ru.py`, `standardize_en.py`,
`geo_out.txt`, `deploy-temp/`, `deploy-temp2`). Итог — полуфабрикат, который
застопорился и не задокументирован:

1. **Порча кодировки (mojibake) — главная беда.** В **199 из 202** изменённых файлов
   юникод испорчен двойным перекодированием (UTF-8 → cp1251/1252): `вЂ”` вместо
   тире, `Д±`/`Еџ` вместо ı/ğ, `MГјdaxilЙ™` вместо «Müdaxilə», BOM `﻿` в началах строк.
   Особенно тяжело пострадали `_supplements/*.html` (34 файла): там ВЕСЬ текст — mojibake.
   В `tehsil.html` испорчена даже строка JS-формы: `service:'Kitab sifariЕџi'`.
   Виноват не сам `standardize_*.py` (он читает/пишет utf-8 корректно) — второй проход
   скрипта с заголовками писал файлы не в той кодировке.
2. **Рефактор hero-разметки накатан наполовину:** новые классы `h1-w1/h1-w2` стоят
   в 223 файлах, но `ph-badge` остался в 211, а старый `ph-h1-w1` — в 7 файлах.
3. **Смена уровней заголовков** (h2→h1, h3→h4 в `_supplements/`, h2→h1 в модалке
   заказа книги в `_partials/kitab-modal.html` + перерисована во все страницы) —
   сомнительная по канону правка, применена вслепую всем файлам.
4. **Классы `.btn-action-1/.btn-action-2`** добавлены в `gtc.css`, но используются
   только в `xidmetler.html` — раскатка брошена на первом файле.
5. **Обрыв процесса:** ни `PROGRESS.md`, ни `HISTORY.json` (последняя запись —
   2026-09-05) не содержат ни слова о работе 6–7 сентября — сессия, которая её вела,
   отчитаться не успела/не стала. Сторожи проекта (`checkup.py`, `regress.py`,
   `refcheck.py`) по этим правкам не прогонялись.
6. **Не было коммита** — нет точки отката внутри этой работы; рабочая копия = смесь
   годных правок и испорченных файлов, которую нельзя запушить в текущем виде.

## 4. Последние данные из чатов/памяти MiniMax (сентябрь)

- **Сессии MiniMax по ragimoff:** 02.09 (5 шт.), 03.09 (7 шт.), 05.09 (2 шт., 20:18 и
  20:34) — это воркер-сессии Explore (аудиты и инвентаризация). Их итоги уже
  влиты в файлы проекта (PROGRESS/TASKS/AUDIT). Основные волны правок 1.1–1.5
  выполнены и закоммичены 1–5 сентября (см. `PROGRESS.md` и `HISTORY.json`).
- **Инвентаризация из сессии 05.09 20:34** (актуальна): 163 файла с паттерном
  `ph-h1-w[12]`; языковые деревья — корень (az) 60, `ru/` 56, `en/` 55; книга
  `klinik-psixiatriya/` — en/ru/tr по 161 карточке; CSS-токены ритма в `gtc.css`
  (`--s-gap-title:16px`, `--s-gap-sub:30px`, `--s-gap-tag-hero:30px`);
  `fix_rhythm.py` снимает инлайн-вертикальные маржины; `lang_tags.py` — авторитетный
  инструмент языковых спанов.
- **Хвосты GEO (TAILS.md, требует Кенана):** п.1 — публикация GEO-набора решена
  (коммит `2b1b9df` запушен); п.2 — IndexNow: после деплоя ключ
  `https://ragimoff.org/indexnow_key.txt` должен открыться, затем перезапустить
  `indexnow_submit.py` (Bing отдавал 403 SiteVerificationNotCompleted, Яндекс уже
  принял 202); п.3 — Google Indexing API не настроен; п.4 — sitemap-пинги устарели;
  п.5 — Review/Rating schema; п.6 — dev-артефакты (`template.html` и `ru/template.html`
  отслеживаются и УЙДУТ НА ПРОД при следующем деплое — решить); п.7 — реестры
  `archive/config` просмотрены, чужих креденшалов нет.
- **Правила Кенана из памяти MiniMax (для продолжения дизайн-работ):**
  - Приоритеты (01.09): 1) бренд/меню/интерфейс, 2) бэкенд, 3) бэкап.
  - 6 правил визуала (03.09): не выкатывать сырое (уровень $5–10K); скелетная
    анимация; перспектива = нарратив; research → код; hero = «влюбись с 1-й строки»;
    5 вариантов = 5 разных исполнений.
  - Перед серьёзным дизайном — скиллы в `~/.minimax/skills/` (пакет
    web-design-mastery-pack, 8 скиллов) + DESIGN.md и PRD.md до кода.
  - Общение — только на русском (07.09).
  - Эталон визуала — постеры Zarifa Ibrahimova (тёмный фон + золото, 3 уровня
    типографики, асимметрия, воздух).

## 5. Рекомендуемый порядок восстановления (на решение Кенана)

1. **Сохранить стэш на всякий случай:** `git stash push -u -m "wip-2026-09-06-design-batch"`.
2. **Откатить испорченное кодировкой:** восстановить из HEAD файлы, где mojibake
   (`git restore` по списку из `git grep -l "вЂ”\|Д±\|Еџ"`), либо целиком
   `git restore .` и заново накатить только нужные правки (gtc.css btn-action,
   hero-унификация) уже правильным скриптом.
3. **Дорешать про заголовки** h2/h3 в `_supplements/` и модалке заказа книги —
   согласовывать с каноном книги, не вслепую.
4. **Докатить или откатить** hero-рефактор (ph-badge → badge) — 211 файлов ещё на старых классах.
5. Прогнать сторожей: `python checkup.py`, `python regress.py`, `python refcheck.py`.
6. Записать сессию в `PROGRESS.md` + `HISTORY.json` (ритуал проекта).
7. Коммит + push → сайт обновится (доступы проверены, деплой исправен).
8. Только после пуша — IndexNow (`indexnow_submit.py`) и остальное из TAILS.md п.2–6.

---

## 6. Исполнение (2026-09-08, ZCode)

Выполнено в тот же день, по п.5 — с одним важным уточнением из глубокой
диагностики:

- **У батча найден второй дефект, исключивший «докатку»:** переименование
  спанов (`ph-h1-w1 → h1-w1`, `ph-badge → badge`) сделано БЕЗ обновления
  CSS-селекторов — `gtc.css` знает только `.ph-h1-w1/w2` (включая
  `:not(.ph-h1-w1)` в правиле контраста на строке 3915), а инлайн-стили
  страниц ссылаются на `.page-hero-x .ph-h1 .ph-h1-w1`. Переименованные
  спаны потеряли стили — двухцветный h1 сломан на затронутых страницах.
  Завершение рефактора = новая волна дизайна (CSS-слой + все страницы).
- **Бэкап:** `_wip_backup_20260906/` (в `.gitignore`): `tracked-changes.patch`
  (17 267 строк), `status-before.txt`, копии артефактов; плюс
  `git stash@{0}` «wip-2026-09-06-design-batch…» (прежние стэши сдвинулись
  на позиции 1–3).
- **Откат:** `git stash push` (без -u) — рабочая копия = HEAD; незакоммиченными
  остались `standardize_az/ru/en.py`, `geo_out.txt`, `deploy-temp/`,
  `deploy-temp2`.
- **Сторожи — все чистые:** checkup 0/19 · regress 1292 · refcheck 0 ·
  numcheck 0/312 · paracheck 0/0 · xrefcheck 0. Mojibake-греп по
  отслеживаемым файлам: 0.
- **Коммиты:** `bbc16ae` (.gitignore: `_wip_backup_*/`) + docs-коммит
  (PROGRESS.md, HISTORY.json, этот файл). Запушено в `origin/main`.
- **kitab-modal** остаётся h2 (вариант батча с h1 — семантическая ошибка).
- **Хронология батча уточнена:** `_supplements/` (33 файла) испорчены
  05.09 23:03, страницы (161) — 06.09 03:14, ручные правки 7 файлов —
  06.09 днём и 07.09 ~01:12.
